import {FC} from 'react';
import {Text} from 'react-native-paper';
import styles from './styles';

const Discover: FC = () => {
  return <Text style={styles.flatlist}>Discover</Text>;
};

export default Discover;
